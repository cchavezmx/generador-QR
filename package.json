{
  "name": "@example/basics",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "wrangler": "npx wrangler pages dev ./dist",
    "prod:test": "npm run build && npm run wrangler",
    "publish": "npm run build && npx wrangler pages publish dist"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^2.0.0",
    "@astrojs/tailwind": "^2.0.1",
    "astro": "^1.3.1",
    "qr-image": "^3.2.0",
    "qrcode": "^1.5.1",
    "tailwindcss": "^3.1.8"
  }
}
